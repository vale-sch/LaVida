<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"  
             x:Class="LaVida.Views.ChatsOverviewPage"
             Title="{Binding Title}"
             xmlns:local="clr-namespace:LaVida.ViewModels"  
             xmlns:model="clr-namespace:LaVida.Models"
             x:Name="BrowseConnectionPage"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
             ios:Page.UseSafeArea="true"
             >
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Command="{Binding AddConnectionCommand}" />
    </ContentPage.ToolbarItems>
    <RefreshView x:DataType="local:ChatsViewModel" Command="{Binding LoadConnectionsCommand}" IsRefreshing="{Binding IsBusy, Mode=Default}" IsEnabled="False">
        <CollectionView x:Name="ItemsListView"
                ItemsSource="{Binding Connections}"
                SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Spacing="8" HorizontalOptions="EndAndExpand"   >
                        <Frame Style="{StaticResource outerFrame}">
                            <Frame Style="{StaticResource innerFrame}">
                            <StackLayout Style="{StaticResource itemsCardViewStackLayoutStyle}">
                                <StackLayout >

                                    <StackLayout Padding="25" x:DataType="model:Connection" >
                                        <Label Text="{Binding ChatPartner}" 
                                               LineBreakMode="NoWrap" 
                                               Style="{StaticResource NameLabel}"
                                               />
                                        <StackLayout.GestureRecognizers>
                                            <TapGestureRecognizer 
                                                NumberOfTapsRequired="1"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type local:ChatsViewModel}}, Path=ConnectionTapped}"		
                                                CommandParameter="{Binding .}">
                                            </TapGestureRecognizer>
                                        </StackLayout.GestureRecognizers>
                                    </StackLayout>
                                </StackLayout>
                            </StackLayout>
                        </Frame>
                        </Frame>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </RefreshView>
</ContentPage>